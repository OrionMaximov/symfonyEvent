{% extends "base.html.twig" %}

{% block body %}

{{form_start(formulaire)}}
{{form_widget(formulaire)}}
{% if mode %}
<button type="submit" class="btn btn-light">modifier un produit</button>
{% else %}
<button type="submit" class="btn btn-light">crée un produit</button>
{% endif %}
{{form_end(formulaire)}}

  
{% endblock body %}